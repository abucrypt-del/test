version: '3.8'

services:
  alyazi-mandi:
    image: nginx:alpine
    container_name: alyazi-mandi-pos
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html
    restart: unless-stopped
    environment:
      - TZ=Asia/Singapore
